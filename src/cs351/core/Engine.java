package cs351.core;

/**
 * The Engine is the primary class that ties together all of the other classes.
 * It will maintain references to a current World, Renderer, SoundEngine and will
 * do things like update the state of the game each frame.
 */
// @todo: Need to finish this interface - not sure exactly all it will need
public interface Engine
{
}
